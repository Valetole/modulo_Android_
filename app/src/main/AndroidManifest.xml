<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cl.valentina.miapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".PantallaInicioSesion"></activity>
        <activity android:name=".MainActivityRecycler">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".view.InicioSesion" />
        <activity android:name=".view.CrearRegistro" />
        <activity android:name=".view.SolicitudDePedido" />
        <activity android:name=".view.MainActivity2" />
        <activity android:name=".FormularioNuevoCliente" />
        <activity android:name=".PantallaInvitadx" />
        <activity android:name=".PantallaCrearRegistro" />
        <activity
            android:name=".SplashScreen"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar.Fullscreen">

        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>